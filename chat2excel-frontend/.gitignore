# ==============================================================================
# .gitignore - Git 忽略文件配置
# ==============================================================================
# 
# 本文件定义了 Git 版本控制中需要忽略的文件和目录。
# 
# Git 会忽略这些文件，不会将它们提交到仓库中。
# 这些文件通常是：
# - 依赖包（node_modules）
# - 构建产物（dist, build）
# - 环境变量（.env）
# - 日志文件
# - 编辑器配置
# - 系统生成的文件
#
# ==============================================================================

# 日志文件 - npm、yarn、pnpm、lerna 等包管理器的日志
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error-log*
pnpm-debug.log*
lerna-debug.log*

# 依赖包目录 - node_modules 包含所有项目依赖，体积很大且可通过 npm install 恢复
node_modules

# 构建产物 - 打包编译后的文件，可通过构建命令重新生成
dist
dist-ssr
build

# 本地文件 - 包含本地特定的文件，不应提交到仓库
*.local

# VS Code 编辑器配置 - 忽略 .vscode 目录下的所有文件
.vscode/*
# 但保留以下配置文件，方便团队成员共享编辑器设置
!.vscode/extensions.json     # 推荐的 VS Code 扩展列表
!.vscode/settings.json        # 编辑器设置
!.vscode/tasks.json           # 任务配置
!.vscode/launch.json         # 调试配置
!.vscode/snippets.json        # 代码片段

# 其他编辑器生成的文件
*.suo          # Visual Studio
*.ntvs*        # Visual Studio Code
*.njsproj      # Visual Studio Node.js 项目文件
*.sln          # Visual Studio 解决方案文件
*.sw?          # Vim 交换文件

# 环境变量文件 - 包含敏感信息（API密钥、数据库密码等），不应提交
.env
.env.local
.env.production.local
.env.development.local
.env.test.local
.env*.local

# 测试覆盖率文件
coverage
.nyc_output

# 系统生成的文件 - macOS 和 Windows 系统生成的隐藏文件
.DS_Store     # macOS Finder 生成的文件
*.pem         # 私钥文件

# Vite 缓存目录
.vite
